import 'package:flutter/material.dart';

class Authlayout extends StatelessWidget {
  const Authlayout({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Authlayout'),
     )
    );
  }
}
